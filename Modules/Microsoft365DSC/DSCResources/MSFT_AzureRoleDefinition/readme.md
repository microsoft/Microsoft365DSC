# AzureRoleDefinition

## Description

This resource configures Azure RBAC custom role definitions.
This resource only manages custom role definitions, not built-in roles.
The account used must have sufficient permissions to manage role definitions,
such as "Owner" or "User Access Administrator" at the appropriate scope.
