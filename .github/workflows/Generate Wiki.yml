name: Generate Wiki Content
on:
  push:
    branches:
      - master
      - Dev

jobs:
  GenerateWiki:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install Modules
        shell: powershell
        run: |
          git clone https://github.com/PowerShell/DscResource.Tests
          Import-Module -Name "./DscResource.Tests/AppVeyor.psm1"
          Invoke-AppveyorInstallTask
          $env:APPVEYOR_BUILD_FOLDER = Get-Location | select -ExpandProperty Path
          $env:APPVEYOR_BUILD_VERSION = "1.0.0"
          Invoke-AppveyorAfterTestTask `
               -Type 'Wiki' `
               -MainModulePath 'Modules\Microsoft365DSC' `
               -ResourceModuleName 'Microsoft365DSC'
      - name: Extract some files
        run: 7z x Microsoft365DSC_1.0.0_wikicontent.zip
      - name: loop files
        run: tree /f
      - name: Move AAD Files
        run: move /Y D:\AAD*.md D:\docs\docs\resources\azure-ad
      - name: Move EXO Files
        run: move EXO*.md \docs\docs\resources\exchange
      - name: Move Intune Files
        run: move Intune*.md \docs\docs\resources\intune
      - name: Move office365 Files
        run: move O365*.md \docs\docs\resources\office365
      - name: Move onedrive Files
        run: move OD*.md \docs\docs\resources\onedrive
      - name: Move planner Files
        run: move Planner*.md \docs\docs\resources\planner
      - name: Move power-platform Files
        run: move PP*.md \docs\docs\resources\power-platform
      - name: Move security-compliance Files
        run: move SC*.md \docs\docs\resources\security-compliance
      - name: Move SharePoint Files
        run: move SPO*.md \docs\docs\resources\sharepoint
      - name: Move Teams Files
        run: move Teams*.md \docs\docs\resources\teams
      - name: loop files
        run: tree /f
      - name: Commit files # commit the output folder
        run: |
          git config --local user.email "rick.vanrousselt@outlook.com"
          git config --local user.name "Rick Van Rousselt"
          git add docs/docs/resources/\*
          git commit -m "Add changes"
          git push

        
