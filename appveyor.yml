version: 1.0.0.{build}


install:
    - git clone https://github.com/PowerShell/DscResource.Tests

    - npm --silent install

    - ps: |
        $moduleName = 'Office365DSC'
        $mainModuleFolder = "Modules\$moduleName"
        Install-Module ReverseDSC -RequiredVersion 1.9.3.0 -Force
        Install-Module MSOnline -RequiredVersion 1.1.183.17 -Force
        Install-Module MicrosoftTeams -RequiredVersion 0.9.6 -Force
        Install-Module SharePointPnPPowerShellOnline -RequiredVersion 3.5.1901.0 -Force
        Install-Module Microsoft.Online.SharePoint.PowerShell -RequiredVersion 16.0.8316.0 -Force
        Install-Module AzureAD -RequiredVersion 2.0.2.4 -Force
        Import-Module Office365DSC -Force
        Import-Module -Name "$env:APPVEYOR_BUILD_FOLDER\DscResource.Tests\AppVeyor.psm1"
        Invoke-AppveyorInstallTask

build: off

test_script:
    - ps: |
        Invoke-AppveyorTestScriptTask `
            -Type 'Harness' `
            -MainModulePath $mainModuleFolder `
            -CodeCovIo

after_test:
    - ps: |
        Invoke-AppveyorAfterTestTask `
            -Type 'Wiki' `
            -MainModulePath $mainModuleFolder `
            -ResourceModuleName $moduleName
